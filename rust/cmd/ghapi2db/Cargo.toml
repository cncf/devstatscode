[package]
name = "ghapi2db"
description = "Sync issues/PRs events, commits, licenses, languages and restore comments/reviews/forks/releases/stars from the GitHub API — Rust port of cmd/ghapi2db"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true

[[bin]]
name = "ghapi2db"
path = "src/main.rs"

[dependencies]
devstatscode = { workspace = true }
chrono = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }

[dev-dependencies]
devstats-compat = { workspace = true }
regex = { workspace = true }
tempfile = { workspace = true }
