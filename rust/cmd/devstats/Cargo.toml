[package]
name = "devstats"
description = "Sync all projects from projects.yaml (get_repos, gha2db_sync per project, website_data) — Rust port of cmd/devstats"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true

[[bin]]
name = "devstats"
path = "src/main.rs"

[dependencies]
devstatscode = { workspace = true }

[dev-dependencies]
devstats-compat = { workspace = true }
tempfile = { workspace = true }
