[package]
name = "columns"
description = "Ensure TSDB series tables have a column per tag value (columns.yaml) — Rust port of cmd/columns"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true

[[bin]]
name = "columns"
path = "src/main.rs"

[dependencies]
devstatscode = { workspace = true }
serde = { workspace = true }

[dev-dependencies]
devstats-compat = { workspace = true }
tempfile = { workspace = true }
