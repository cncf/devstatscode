[package]
name = "merge_dbs"
description = "Merge multiple DevStats project databases into one (shared) database — Rust port of cmd/merge_dbs"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true

[[bin]]
name = "merge_dbs"
path = "src/main.rs"

[dependencies]
devstatscode = { workspace = true }

[dev-dependencies]
devstats-compat = { workspace = true }
tempfile = { workspace = true }
