[package]
name = "webhook"
description = "Travis CI webhook receiver that deploys DevStats on successful builds — Rust port of cmd/webhook"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true

[[bin]]
name = "webhook"
path = "src/main.rs"

[dependencies]
devstatscode = { workspace = true }
serde_json = { workspace = true }
base64 = { workspace = true }
rsa = { workspace = true }
sha1 = { workspace = true }

[dev-dependencies]
devstats-compat = { workspace = true }
tempfile = { workspace = true }
