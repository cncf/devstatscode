[package]
name = "replacer"
description = "In-place regexp/string replacement tool driven by env variables — Rust port of cmd/replacer"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true

[[bin]]
name = "replacer"
path = "src/main.rs"

[dependencies]
devstatscode = { workspace = true }
regex = { workspace = true }
memchr = { workspace = true }

[dev-dependencies]
devstats-compat = { workspace = true }
tempfile = { workspace = true }
